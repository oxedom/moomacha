# Shared fixtures live with the tests that use them. Each test module builds the
# app/router it needs via the route-builder factories, so no global app fixture
# is required here. (Intentionally minimal — no global fixtures needed.)
